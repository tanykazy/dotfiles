#!/bin/bash

# Swap CapsLock and Ctrl
#setxkbmap -option ctrl:swapcaps

# Start xbindkeys
xbindkeys_autostart

# Status text for dwm
while true
do
	LOCALTIME="$(date)"
	BATTERY="$(acpi -b)"
	xsetroot -name "${LOCALTIME} ${BATTERY}"
#	wmname "$LOCALTIME"
#	slstatus
	sleep 1s
done &

